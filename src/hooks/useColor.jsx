import { useState } from "react";

// const getColor = (colorItem) =>
//   colorItem.map((item) => ({
//     name: item,
//     status: false,
//   }));

// export function useColor(jacket) {
//   const [stateColor, setStateColor] = useState([]);
//   const windowColor = jacket.colorItem ? getColor(jacket.colorItem) : [];
  //const windowColor = stateColor ? getColor(jacket.colorItem) : [];
//   const [color, setColor] = useState(windowColor);
//   const checkedColor = (index) => {
//     setColor(
//       color.map((item, i) => {
//         if (i === index) {
//           item.status = !item.status;
//         }
//         return item;
//       })
//     );
//   };
//   return { color, checkedColor };
// }
